mixin plans-item(title, count, badge, alignment)
    div(class="plans-item " + "item-" + count + " " + alignment)
        .title
            p #{title}
        div(class="count " + alignment)
            p #{count}
        div(class="badge " + alignment)
            p #{badge}